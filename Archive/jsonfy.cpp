#include <iostream>
#include <string>

class Jsonfy {
private:
  /*
    pointers for parsing txt file go here?
  */

public:
  void init(/* arguments */)
  {

  }

  void add()
  {
    /*
    Adding the next element to a JSON list
      read json text
      parse by the chars we're looking for  ('}') perhaps use string library?)
      make some modifications after the char of interest (add a comma, new line, 2 indentation)
      write the the next json object by calling init() to add the new element
    */
  }


};
